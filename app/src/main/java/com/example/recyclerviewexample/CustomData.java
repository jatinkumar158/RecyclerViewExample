package com.example.recyclerviewexample;

// We are displaying image and text so to hold different type of data we use class
// If we just need to display same kind of data we can use array

public class CustomData {

    int image;
    String text;

    public CustomData(int image, String text) {
        this.image = image;
        this.text = text;
    }
}
